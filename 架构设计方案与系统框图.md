# RISC-V CPU 架构设计方案与系统框图

> 基于 Nexys 4 DDR (Artix-7 XC7A100T) 的 RISC-V 处理器设计
> 课程：项目式课程阶段2 — 题目B：基于FPGA开发板的处理器设计

---

## 目录

1. [架构方案对比总览](#1-架构方案对比总览)
2. [方案一：单周期 RISC-V CPU（基础层次）](#2-方案一单周期-risc-v-cpu基础层次)
3. [方案二：多周期 RISC-V CPU（基础层次）](#3-方案二多周期-risc-v-cpu基础层次)
4. [方案三：五级流水线 RISC-V CPU（进阶层次）](#4-方案三五级流水线-risc-v-cpu进阶层次)
5. [方案四：完整 SoC 系统架构（进阶层次）](#5-方案四完整-soc-系统架构进阶层次)
6. [系统框图集合](#6-系统框图集合)
7. [方案选择建议](#7-方案选择建议)

---

## 1. 架构方案对比总览

### 1.1 四种架构方案对比

| 维度 | 方案一：单周期 | 方案二：多周期 | 方案三：五级流水线 | 方案四：完整SoC |
|------|-------------|-------------|----------------|--------------|
| **CPI** | 1 | 3~5 | 理想1（实际1.2~1.5） | 理想1（含Cache miss开销） |
| **时钟频率** | 低（~25MHz） | 中（~50MHz） | 高（~100MHz） | 高（~80MHz） |
| **复杂度** | 低 | 中 | 高 | 很高 |
| **开发周期** | 1~2周 | 2~3周 | 3~4周 | 5~6周 |
| **关键路径** | 取指→译码→执行→访存→写回 | 各阶段拆分 | 流水线寄存器 | 总线+Cache |
| **资源占用** | 少 | 少 | 中 | 多 |
| **适用层次** | 基础 | 基础 | 进阶 | 进阶 |
| **RV32I支持** | 完整 | 完整 | 完整 | 完整+M扩展 |

### 1.2 架构演进路线

```mermaid
graph LR
    A["方案一<br/>单周期CPU<br/>(基础)"] --> B["方案二<br/>多周期CPU<br/>(基础)"]
    B --> C["方案三<br/>五级流水线<br/>(进阶)"]
    C --> D["方案四<br/>完整SoC<br/>(进阶)"]

    A -->|可选直接跳跃| C
    B -->|可选直接跳跃| D

    style A fill:#4CAF50,color:#fff
    style B fill:#4CAF50,color:#fff
    style C fill:#FF9800,color:#fff
    style D fill:#FF9800,color:#fff
```

### 1.3 整体设计层次结构

```mermaid
graph TD
    subgraph 课程要求
        L1["基础层次：简单CPU设计"]
        L2["进阶层次：完整计算机系统"]
        L3["拓展层次：高性能优化（暂不做）"]
    end

    subgraph 基础层次交付物
        B1["RV32I指令集实现"]
        B2["数据通路设计"]
        B3["控制器设计"]
        B4["功能仿真验证"]
        B5["FPGA硬件验证"]
    end

    subgraph 进阶层次交付物
        A1["流水线CPU"]
        A2["Cache存储系统"]
        A3["I/O接口"]
        A4["总线互联"]
        A5["性能量化评估"]
    end

    L1 --> B1 & B2 & B3 & B4 & B5
    L2 --> A1 & A2 & A3 & A4 & A5
    L1 --> L2

    style L1 fill:#4CAF50,color:#fff
    style L2 fill:#FF9800,color:#fff
    style L3 fill:#9E9E9E,color:#fff
```

---

## 2. 方案一：单周期 RISC-V CPU（基础层次）

### 2.1 架构概述

单周期CPU在单个时钟周期内完成一条指令的完整执行。所有指令共用同一时钟周期，时钟周期由最慢指令（Load指令）决定。

- **指令集**：RV32I 子集（37条核心指令）
- **数据通路**：组合逻辑为主，无流水线寄存器
- **存储器**：哈佛结构（指令存储器与数据存储器分离）
- **控制器**：纯组合逻辑，根据opcode生成控制信号

### 2.2 数据通路框图

```mermaid
graph TD
    PC["PC寄存器<br/>32位"]
    PC_PLUS4["PC+4 加法器"]
    IMEM["指令存储器<br/>IMEM<br/>(ROM)"]

    REG_FILE["寄存器堆<br/>x0~x31<br/>32×32位<br/>2读1写"]

    IMM_GEN["立即数生成器<br/>ImmGen"]

    ALU["ALU<br/>算术逻辑单元"]

    DMEM["数据存储器<br/>DMEM<br/>(RAM)"]

    MUX_ALU1{"Mux<br/>ALUSrcA"}
    MUX_ALU2{"Mux<br/>ALUSrcB"}
    MUX_WD{"Mux<br/>MemToReg"}
    MUX_PC{"Mux<br/>PCSource"}

    BRANCH["分支控制<br/>Branch Control"]

    CTRL["控制器<br/>Control Unit"]

    PC --> IMEM
    PC --> PC_PLUS4
    PC_PLUS4 --> MUX_PC
    IMEM -->|instruction| REG_FILE
    IMEM -->|instruction| IMM_GEN
    IMEM -->|opcode| CTRL

    CTRL -->|控制信号| ALU
    CTRL -->|控制信号| DMEM
    CTRL -->|控制信号| REG_FILE
    CTRL -->|控制信号| BRANCH
    CTRL -->|控制信号| MUX_ALU1
    CTRL -->|控制信号| MUX_ALU2
    CTRL -->|控制信号| MUX_WD
    CTRL -->|控制信号| MUX_PC

    REG_FILE -->|rs1| MUX_ALU1
    REG_FILE -->|rs2| MUX_ALU2
    IMM_GEN -->|imm| MUX_ALU2

    MUX_ALU1 -->|operand A| ALU
    MUX_ALU2 -->|operand B| ALU

    ALU -->|result| DMEM
    ALU -->|result| MUX_PC
    ALU -->|zero/lt| BRANCH

    DMEM -->|read data| MUX_WD
    MUX_WD -->|write data| REG_FILE

    BRANCH -->|branch taken| MUX_PC
    ALU -->|branch target| MUX_PC
    MUX_PC -->|next PC| PC

    style PC fill:#2196F3,color:#fff
    style CTRL fill:#FF5722,color:#fff
    style ALU fill:#4CAF50,color:#fff
    style REG_FILE fill:#9C27B0,color:#fff
    style IMEM fill:#FF9800,color:#fff
    style DMEM fill:#FF9800,color:#fff
```

### 2.3 控制信号表

```mermaid
graph TD
    subgraph 控制信号生成
        OP["opcode[6:0]"]
        CTRL["控制单元<br/>Control Unit"]
    end

    subgraph 主控制信号
        RW["RegWrite<br/>寄存器写使能"]
        AS1["ALUSrcA<br/>ALU操作数A来源"]
        AS2["ALUSrcB<br/>ALU操作数B来源"]
        ALU_OP["ALUOp<br/>ALU操作类型"]
        MR["MemRead<br/>存储器读"]
        MW["MemWrite<br/>存储器写"]
        MT["MemToReg<br/>写回数据来源"]
        BR["Branch<br/>分支控制"]
        JS["Jump<br/>跳转控制"]
    end

    OP --> CTRL
    CTRL --> RW & AS1 & AS2 & ALU_OP & MR & MW & MT & BR & JS

    style CTRL fill:#FF5722,color:#fff
    style OP fill:#2196F3,color:#fff
```

### 2.4 关键模块设计

```mermaid
graph LR
    subgraph ALU功能
        A1["加法 ADD"]
        A2["减法 SUB"]
        A3["按位与 AND"]
        A4["按位或 OR"]
        A5["按位异或 XOR"]
        A6["小于比较 SLT"]
        A7["左移 SLL"]
        A8["右移 SRL/SRA"]
    end

    subgraph 指令类型
        T1["R型: add/sub/and/or/xor/slt/sll/srl/sra"]
        T2["I型: addi/andi/ori/xori/slti/slli/srli/srai"]
        T3["Load: lb/lh/lw/lbu/lhu"]
        T4["Store: sb/sh/sw"]
        T5["Branch: beq/bne/blt/bge/bltu/bgeu"]
        T6["Jump: jal/jalr"]
        T7["LUI/AUIPC"]
    end

    style A1 fill:#4CAF50,color:#fff
    style A2 fill:#4CAF50,color:#fff
    style T1 fill:#2196F3,color:#fff
    style T3 fill:#FF9800,color:#fff
    style T5 fill:#9C27B0,color:#fff
```

### 2.5 优缺点分析

| 优点 | 缺点 |
|------|------|
| 结构简单，易于理解和调试 | 时钟周期长（由最慢指令决定） |
| 每条指令CPI=1 | 关键路径长，频率低 |
| 无冒险问题 | 资源利用率低（ALU每周期空闲部分时间） |
| 适合快速验证指令集正确性 | 不适合实际产品应用 |

---

## 3. 方案二：多周期 RISC-V CPU（基础层次）

### 3.1 架构概述

多周期CPU将指令执行拆分为多个时钟周期，每个周期完成一个子操作。不同指令使用不同周期数。

- **指令集**：RV32I 子集
- **数据通路**：共享存储器（冯·诺依曼结构），单存储器分时复用
- **控制器**：有限状态机（FSM）驱动
- **周期分配**：取指(IF) → 译码(ID) → 执行(EX) → 访存(MEM) → 写回(WB)

### 3.2 控制器状态机

```mermaid
stateDiagram-v2
    [*] --> S_FETCH: reset

    S_FETCH: 取指状态<br/>IF<br/>IR ← Mem[PC]
    S_DECODE: 译码状态<br/>ID<br/>解码, 读寄存器
    S_EXEC: 执行状态<br/>EX<br/>ALU运算
    S_MEM_ADDR: 访存地址<br/>计算访存地址
    S_MEM_READ: 读存储器<br/>MEM_R
    S_MEM_WRITE: 写存储器<br/>MEM_W
    S_WRITEBACK: 写回状态<br/>WB<br/>结果写回寄存器
    S_BRANCH: 分支判断
    S_JUMP: 跳转执行
    S_LUI: LUI执行

    S_FETCH --> S_DECODE: 完成取指

    S_DECODE --> S_EXEC: R型/I型
    S_DECODE --> S_MEM_ADDR: Load/Store
    S_DECODE --> S_BRANCH: Branch
    S_DECODE --> S_JUMP: JAL/JALR
    S_DECODE --> S_LUI: LUI/AUIPC

    S_EXEC --> S_FETCH: R型/I型完成

    S_MEM_ADDR --> S_MEM_READ: Load
    S_MEM_ADDR --> S_MEM_WRITE: Store
    S_MEM_READ --> S_WRITEBACK
    S_MEM_WRITE --> S_FETCH
    S_WRITEBACK --> S_FETCH

    S_BRANCH --> S_FETCH: 分支完成
    S_JUMP --> S_FETCH: 跳转完成
    S_LUI --> S_FETCH: LUI完成
```

### 3.3 数据通路框图

```mermaid
graph TD
    PC["PC寄存器"]
    MUX_PC{"PCSource<br/>Mux"}
    MEM["统一存储器<br/>Memory<br/>(指令+数据)"]
    IR["指令寄存器<br/>IR"]
    MDR["内存数据寄存器<br/>MDR"]

    REG_A["寄存器A<br/>暂存rs1"]
    REG_B["寄存器B<br/>暂存rs2"]

    IMM_GEN["立即数生成器<br/>ImmGen"]

    MUX_ALU_A{"ALUSrcA<br/>Mux"}
    MUX_ALU_B{"ALUSrcB<br/>Mux"}

    ALU["ALU<br/>ALU"]
    ALU_OUT["ALU输出寄存器<br/>ALUOut"]

    REG_FILE["寄存器堆<br/>x0~x31<br/>32×32位"]

    CTRL["有限状态机控制器<br/>FSM Controller"]

    PC --> MUX_PC
    MUX_PC --> MEM
    MEM --> IR
    MEM --> MDR
    MEM -->|地址| MEM

    IR -->|rs1| REG_FILE
    IR -->|rs2| REG_FILE
    IR -->|opcode/funct| CTRL
    IR --> IMM_GEN

    REG_FILE --> REG_A
    REG_FILE --> REG_B

    REG_A --> MUX_ALU_A
    PC --> MUX_ALU_A
    IMM_GEN --> MUX_ALU_B
    REG_B --> MUX_ALU_B
    ALU_OUT --> MUX_ALU_B

    MUX_ALU_A --> ALU
    MUX_ALU_B --> ALU

    ALU --> ALU_OUT
    ALU_OUT --> MEM
    ALU_OUT --> MUX_PC
    ALU --> MUX_PC

    MDR --> REG_FILE
    ALU_OUT --> REG_FILE
    PC --> REG_FILE

    CTRL -->|控制信号| PC
    CTRL -->|控制信号| MEM
    CTRL -->|控制信号| IR
    CTRL -->|控制信号| MUX_ALU_A
    CTRL -->|控制信号| MUX_ALU_B
    CTRL -->|控制信号| ALU
    CTRL -->|控制信号| ALU_OUT
    CTRL -->|控制信号| REG_FILE
    CTRL -->|控制信号| MUX_PC

    style PC fill:#2196F3,color:#fff
    style CTRL fill:#FF5722,color:#fff
    style ALU fill:#4CAF50,color:#fff
    style MEM fill:#FF9800,color:#fff
    style REG_FILE fill:#9C27B0,color:#fff
    style ALU_OUT fill:#795548,color:#fff
    style MDR fill:#795548,color:#fff
```

### 3.4 指令周期数分配

```mermaid
graph TD
    subgraph R型指令 3周期
        R1["周期1: IF<br/>取指令"]
        R2["周期2: ID<br/>译码+读寄存器"]
        R3["周期3: EX<br/>ALU运算+写回"]
        R1 --> R2 --> R3
    end

    subgraph Load指令 5周期
        L1["周期1: IF"]
        L2["周期2: ID"]
        L3["周期3: EX<br/>计算地址"]
        L4["周期4: MEM<br/>读存储器"]
        L5["周期5: WB<br/>写回寄存器"]
        L1 --> L2 --> L3 --> L4 --> L5
    end

    subgraph Store指令 4周期
        S1["周期1: IF"]
        S2["周期2: ID"]
        S3["周期3: EX<br/>计算地址"]
        S4["周期4: MEM<br/>写存储器"]
        S1 --> S2 --> S3 --> S4
    end

    subgraph Branch指令 3周期
        B1["周期1: IF"]
        B2["周期2: ID"]
        B3["周期3: EX<br/>比较+更新PC"]
        B1 --> B2 --> B3
    end

    style R1 fill:#4CAF50,color:#fff
    style L1 fill:#FF9800,color:#fff
    style S1 fill:#2196F3,color:#fff
    style B1 fill:#9C27B0,color:#fff
```

### 3.5 优缺点分析

| 优点 | 缺点 |
|------|------|
| 时钟周期短（单级组合逻辑） | CPI > 1，整体吞吐量不一定高 |
| 不同指令可使用不同周期数 | 控制器FSM复杂 |
| 共享存储器节省资源 | 串行执行，无指令重叠 |
| 关键路径短，频率可较高 | 状态机调试较复杂 |

---

## 4. 方案三：五级流水线 RISC-V CPU（进阶层次）

### 4.1 架构概述

五级流水线将指令执行分为5个阶段，允许多条指令同时在不同阶段执行。

- **指令集**：RV32I 完整 + M扩展（可选）
- **流水线级数**：IF → ID → EX → MEM → WB
- **冒险处理**：数据前递 + Load-Use停顿 + 分支冲刷
- **目标性能**：IPC ≈ 0.85~0.95

### 4.2 流水线架构总图

```mermaid
graph LR
    subgraph IF["IF 取指"]
        PC_IF["PC"]
        PC4_IF["PC+4"]
        IMEM_IF["指令存储器<br/>IMEM"]
        IF_ID["IF/ID<br/>流水线寄存器"]

        PC_IF --> IMEM_IF
        PC_IF --> PC4_IF
        IMEM_IF --> IF_ID
        PC4_IF --> IF_ID
    end

    subgraph ID["ID 译码"]
        DECODE_ID["译码器<br/>Decoder"]
        REGFILE_ID["寄存器堆<br/>RegFile"]
        IMM_ID["立即数生成<br/>ImmGen"]
        HAZARD_ID["冒险检测<br/>Hazard Unit"]
        ID_EX["ID/EX<br/>流水线寄存器"]

        IF_ID --> DECODE_ID
        IF_ID --> REGFILE_ID
        IF_ID --> IMM_ID
        DECODE_ID --> ID_EX
        REGFILE_ID --> ID_EX
        IMM_ID --> ID_EX
        HAZARD_ID -->|stall/flush| IF_ID
        HAZARD_ID -->|stall/flush| ID_EX
    end

    subgraph EX["EX 执行"]
        FWD_EX["前递单元<br/>Forward Unit"]
        MUX_A{"FwdA<br/>Mux"}
        MUX_B{"FwdB<br/>Mux"}
        ALU_EX["ALU"]
        BRANCH_EX["分支判断<br/>Branch Unit"]
        EX_MEM["EX/MEM<br/>流水线寄存器"]

        ID_EX --> MUX_A
        ID_EX --> MUX_B
        ID_EX --> ALU_EX
        ID_EX --> BRANCH_EX
        FWD_EX --> MUX_A
        FWD_EX --> MUX_B
        MUX_A --> ALU_EX
        MUX_B --> ALU_EX
        ALU_EX --> EX_MEM
        BRANCH_EX --> EX_MEM
    end

    subgraph MEM["MEM 访存"]
        DMEM_MEM["数据存储器<br/>DMEM"]
        MEM_WB["MEM/WB<br/>流水线寄存器"]

        EX_MEM --> DMEM_MEM
        EX_MEM --> MEM_WB
        DMEM_MEM --> MEM_WB
    end

    subgraph WB["WB 写回"]
        MUX_WB{"MemToReg<br/>Mux"}
        REGFILE_WB["寄存器堆<br/>写端口"]

        MEM_WB --> MUX_WB
        MUX_WB --> REGFILE_WB
    end

    IF_ID -.-> IF
    ID_EX -.-> ID
    EX_MEM -.-> EX
    MEM_WB -.-> MEM

    REGFILE_WB -.->|写回数据| REGFILE_ID

    style PC_IF fill:#2196F3,color:#fff
    style HAZARD_ID fill:#FF5722,color:#fff
    style FWD_EX fill:#FF5722,color:#fff
    style ALU_EX fill:#4CAF50,color:#fff
    style IMEM_IF fill:#FF9800,color:#fff
    style DMEM_MEM fill:#FF9800,color:#fff
    style REGFILE_ID fill:#9C27B0,color:#fff
```

### 4.3 数据冒险与前递路径

```mermaid
graph TD
    subgraph 冒险类型
        H1["数据冒险<br/>Data Hazard"]
        H2["控制冒险<br/>Control Hazard"]
        H3["结构冒险<br/>Structural Hazard"]
    end

    subgraph 数据冒险解决
        D1["EX→EX 前递<br/>上一条指令ALU结果<br/>→当前指令ALU输入"]
        D2["MEM→EX 前递<br/>上上条指令ALU结果<br/>→当前指令ALU输入"]
        D3["Load-Use 停顿<br/>Load后紧跟依赖指令<br/>插入1个bubble"]
    end

    subgraph 控制冒险解决
        C1["分支冲刷<br/>Branch Flush<br/>清空IF/ID寄存器"]
        C2["静态预测<br/>总是不跳转<br/>(BTFN)"]
        C3["延迟分支<br/>Delay Slot<br/>(RISC-V不推荐)"]
    end

    subgraph 结构冒险解决
        S1["哈佛结构<br/>指令/数据存储器分离<br/>消除存储器竞争"]
        S2["寄存器堆<br/>同时读写<br/>前半周期写/后半周期读"]
    end

    H1 --> D1 & D2 & D3
    H2 --> C1 & C2 & C3
    H3 --> S1 & S2

    style H1 fill:#F44336,color:#fff
    style H2 fill:#FF9800,color:#fff
    style H3 fill:#2196F3,color:#fff
    style D1 fill:#4CAF50,color:#fff
    style C1 fill:#4CAF50,color:#fff
    style S1 fill:#4CAF50,color:#fff
```

### 4.4 前递逻辑详解

```mermaid
graph TD
    subgraph 前递场景1_EX到EX
        EX1_1["指令i: add x5, x6, x7"]
        EX1_2["指令i+1: sub x8, x5, x9"]
        EX1_3["i在EX/MEM阶段时<br/>i+1在EX阶段"]
        EX1_4["前递: EX/MEM.ALUOut → EX输入"]
        EX1_1 --> EX1_2 --> EX1_3 --> EX1_4
    end

    subgraph 前递场景2_MEM到EX
        EX2_1["指令i: add x5, x6, x7"]
        EX2_2["指令i+1: nop"]
        EX2_3["指令i+2: sub x8, x5, x9"]
        EX2_4["i在MEM/WB阶段时<br/>i+2在EX阶段"]
        EX2_5["前递: MEM/WB.ALUOut → EX输入"]
        EX2_1 --> EX2_2 --> EX2_3 --> EX2_4 --> EX2_5
    end

    subgraph LoadUse停顿
        LU1["指令i: lw x5, 0(x6)"]
        LU2["指令i+1: sub x8, x5, x9"]
        LU3["i在MEM阶段时<br/>i+1在EX阶段"]
        LU4["无法前递!<br/>Load数据MEM阶段才出来"]
        LU5["插入1个bubble<br/>stall PC和IF/ID<br/>清除ID/EX控制信号"]
        LU1 --> LU2 --> LU3 --> LU4 --> LU5
    end

    style EX1_4 fill:#4CAF50,color:#fff
    style EX2_5 fill:#4CAF50,color:#fff
    style LU4 fill:#F44336,color:#fff
    style LU5 fill:#FF9800,color:#fff
```

### 4.5 流水线时序图

```mermaid
sequenceDiagram
    participant IF
    participant ID
    participant EX
    participant MEM
    participant WB

    Note over IF,WB: 正常流水线（无冒险）
    IF->>ID: 周期1: I1取指
    IF->>ID: 周期2: I2取指
    ID->>EX: 周期2: I1译码
    IF->>ID: 周期3: I3取指
    ID->>EX: 周期3: I2译码
    EX->>MEM: 周期3: I1执行
    IF->>ID: 周期4: I4取指
    ID->>EX: 周期4: I3译码
    EX->>MEM: 周期4: I2执行
    MEM->>WB: 周期4: I1访存
    ID->>EX: 周期5: I4译码
    EX->>MEM: 周期5: I3执行
    MEM->>WB: 周期5: I2访存
    WB->>WB: 周期5: I1写回

    Note over IF,WB: Load-Use冒险（停顿1周期）
    IF->>ID: 周期6: I5 lw取指
    IF->>ID: 周期7: stall（保持I5）
    ID->>EX: 周期7: I4译码
    Note over EX: bubble（清除控制信号）
    EX->>MEM: 周期7: bubble
    IF->>ID: 周期8: I6取指
    ID->>EX: 周期8: I5 lw译码
    EX->>MEM: 周期8: I4执行
    MEM->>WB: 周期8: I5访存数据就绪→前递

    Note over IF,WB: 分支冲刷
    IF->>ID: 周期9: I7 beq取指
    ID->>EX: 周期9: I7译码
    EX->>MEM: 周期9: I7执行（判定跳转）
    Note over IF,ID: 周期10: flush IF/ID（清空错误指令）
    IF->>ID: 周期10: I_target取指
```

### 4.6 分支处理策略

```mermaid
graph TD
    subgraph 简单方案_分支在EX阶段判定
        E1["IF: 取指"]
        E2["ID: 译码"]
        E3["EX: ALU计算+分支判定"]
        E4["如果跳转: 冲刷IF和ID阶段<br/>损失2个周期"]
        E1 --> E2 --> E3 --> E4
    end

    subgraph 优化方案1_分支提前到ID阶段
        O1["IF: 取指"]
        O2["ID: 译码+分支判定<br/>(专用比较器)"]
        O3["如果跳转: 冲刷IF阶段<br/>损失1个周期"]
        O1 --> O2 --> O3
    end

    subgraph 优化方案2_静态预测
        P1["IF: 取指"]
        P2["ID: 译码+预测"]
        P3["预测不跳转: 继续顺序执行"]
        P4["EX: 实际判定"]
        P5{"预测正确?"}
        P6["是: 无损失"]
        P7["否: 冲刷并跳转"]
        P1 --> P2 --> P3 --> P4 --> P5
        P5 -->|是| P6
        P5 -->|否| P7
    end

    style E4 fill:#F44336,color:#fff
    style O3 fill:#FF9800,color:#fff
    style P3 fill:#4CAF50,color:#fff
    style P7 fill:#F44336,color:#fff
```

### 4.7 优缺点分析

| 优点 | 缺点 |
|------|------|
| 高吞吐量（理想IPC=1） | 设计复杂度高 |
| 时钟频率高（关键路径短） | 冒险处理逻辑复杂 |
| 资源利用率高 | 调试困难（时序相关） |
| 适合进阶层次要求 | 需要仔细验证前递逻辑 |

---

## 5. 方案四：完整 SoC 系统架构（进阶层次）

### 5.1 架构概述

在五级流水线CPU基础上，集成Cache、总线、外设接口，构建完整可运行的计算机系统。

- **CPU核心**：五级流水线 RISC-V (RV32I + M扩展)
- **存储层次**：L1 I-Cache + L1 D-Cache + 主存(BRAM/DDR)
- **总线**：自定义总线或 Wishbone/AXI-Lite
- **外设**：UART、GPIO、Timer、中断控制器

### 5.2 SoC 系统顶层框图

```mermaid
graph TD
    subgraph CPU核心
        CORE["RISC-V CPU Core<br/>五级流水线<br/>RV32I + M扩展"]
    end

    subgraph 存储子系统
        ICACHE["指令Cache<br/>I-Cache<br/>直接映射<br/>4KB / 16B行"]
        DCACHE["数据Cache<br/>D-Cache<br/>直接映射<br/>4KB / 16B行"]
        MAIN_MEM["主存储器<br/>Main Memory<br/>BRAM 64KB<br/>(或DDR)"]
    end

    subgraph 总线互联
        BUS["总线互联矩阵<br/>Bus Interconnect<br/>(Wishbone/AXI-Lite)"]
        ARB["总线仲裁器<br/>Arbiter"]
    end

    subgraph 外设接口
        UART["UART<br/>串口控制器<br/>115200 baud"]
        GPIO["GPIO<br/>通用IO<br/>LED/按键/开关"]
        TIMER["Timer<br/>定时器<br/>中断源"]
        PLIC["PLIC<br/>中断控制器"]
        SPI["SPI<br/>(可选)"]
        I2C["I2C<br/>(可选)"]
    end

    subgraph FPGA板载资源
        CLK["时钟管理<br/>MMCM/PLL<br/>100MHz"]
        RST["复位管理<br/>Reset Controller"]
        LEDS["LED ×16"]
        SWITCHES["拨码开关 ×16"]
        BUTTONS["按键 ×5"]
        SEG["七段数码管 ×8"]
        USB_UART["USB-UART<br/>Bridge"]
    end

    CORE -->|取指请求| ICACHE
    CORE -->|访存请求| DCACHE

    ICACHE -->|miss时| BUS
    DCACHE -->|miss时| BUS

    BUS --> MAIN_MEM
    BUS --> UART
    BUS --> GPIO
    BUS --> TIMER
    BUS --> PLIC
    BUS --> SPI
    BUS --> I2C

    ARB --> BUS

    PLIC -->|中断| CORE
    TIMER -->|中断| PLIC
    UART -->|中断| PLIC
    GPIO -->|中断| PLIC

    CLK --> CORE
    CLK --> ICACHE
    CLK --> DCACHE
    CLK --> BUS
    RST --> CORE

    UART --> USB_UART
    GPIO --> LEDS
    GPIO --> SWITCHES
    GPIO --> BUTTONS
    GPIO --> SEG

    style CORE fill:#2196F3,color:#fff
    style ICACHE fill:#4CAF50,color:#fff
    style DCACHE fill:#4CAF50,color:#fff
    style BUS fill:#FF5722,color:#fff
    style MAIN_MEM fill:#FF9800,color:#fff
    style PLIC fill:#9C27B0,color:#fff
    style CLK fill:#607D8B,color:#fff
```

### 5.3 存储层次结构

```mermaid
graph TD
    subgraph 存储层次
        L1I["L1 I-Cache<br/>指令缓存<br/>4KB, 直接映射<br/>16B/行, 4路组相联"]
        L1D["L1 D-Cache<br/>数据缓存<br/>4KB, 直接映射<br/>16B/行, 写回策略"]
        L2["L2 统一Cache<br/>(可选)<br/>32KB, 4路组相联"]
        MAIN["主存<br/>BRAM 64KB<br/>或 DDR3 128MB"]
        FLASH["Flash<br/>(启动存储)"]
    end

    subgraph 访问延迟
        T1["L1: 1~2 cycle"]
        T2["L2: 5~10 cycle"]
        T3["Main: 20~50 cycle"]
        T4["Flash: 100+ cycle"]
    end

    CPU["CPU核心"] --> L1I
    CPU --> L1D
    L1I --> L2
    L1D --> L2
    L2 --> MAIN
    MAIN --> FLASH

    L1I -.-> T1
    L1D -.-> T1
    L2 -.-> T2
    MAIN -.-> T3
    FLASH -.-> T4

    style L1I fill:#4CAF50,color:#fff
    style L1D fill:#4CAF50,color:#fff
    style L2 fill:#8BC34A,color:#fff
    style MAIN fill:#FF9800,color:#fff
    style FLASH fill:#795548,color:#fff
    style CPU fill:#2196F3,color:#fff
```

### 5.4 Cache 结构设计

```mermaid
graph TD
    subgraph I-Cache结构
        ITAG["Tag阵列<br/>Tag Array<br/>直接映射"]
        IDATA["Data阵列<br/>Data Array<br/>16B/行"]
        ICTRL["Cache控制器<br/>命中/缺失判断"]
        IREP["替换策略<br/>(直接映射无需)"]
        IFIFO["预取缓冲<br/>Prefetch Buffer<br/>(可选)"]
    end

    subgraph D-Cache结构
        DTAG["Tag阵列<br/>Tag Array<br/>直接映射"]
        DDATA["Data阵列<br/>Data Array<br/>16B/行"]
        DCTRL["Cache控制器<br/>命中/缺失判断"]
        DWRITE["写策略<br/>写回 Write-Back<br/>写分配 Write-Allocate"]
        DDIR["脏位<br/>Dirty Bit"]
        DVALID["有效位<br/>Valid Bit"]
    end

    subgraph Cache缺失处理
        M1["Cache Miss检测"]
        M2["向主存发请求<br/>读取整个Cache行"]
        M3["等待主存响应<br/>CPU stall"]
        M4["写入Cache行<br/>更新Tag/Data"]
        M5["返回数据给CPU"]
        M1 --> M2 --> M3 --> M4 --> M5
    end

    style ITAG fill:#4CAF50,color:#fff
    style IDATA fill:#4CAF50,color:#fff
    style DTAG fill:#4CAF50,color:#fff
    style DDATA fill:#4CAF50,color:#fff
    style DWRITE fill:#FF9800,color:#fff
    style M3 fill:#F44336,color:#fff
```

### 5.5 总线协议与互联

```mermaid
graph TD
    subgraph 总线主设备 Masters
        CPU_M["CPU<br/>(指令取数)"]
        CPU_D["CPU<br/>(数据访存)"]
        DMA["DMA<br/>(可选)"]
    end

    subgraph 总线仲裁
        ARB["仲裁器<br/>Arbiter<br/>轮询优先级"]
    end

    subgraph 总线从设备 Slaves
        MEM_S["主存<br/>0x00000000<br/>~0x0000FFFF"]
        UART_S["UART<br/>0x10000000<br/>~0x1000000F"]
        GPIO_S["GPIO<br/>0x10010000<br/>~0x1001000F"]
        TIMER_S["Timer<br/>0x10020000<br/>~0x1002000F"]
        PLIC_S["PLIC<br/>0x0C000000<br/>~0x0CFFFFFF"]
    end

    subgraph 总线信号
        SIG_ADR[" adr[31:0]<br/>地址总线"]
        SIG_DAT_W[" dat_w[31:0]<br/>写数据"]
        SIG_DAT_R[" dat_r[31:0]<br/>读数据"]
        SIG_WE[" we<br/>写使能"]
        SIG_SEL[" sel[3:0]<br/>字节选择"]
        SIG_CYC[" cyc<br/>总线周期"]
        SIG_STB[" stb<br/>选通"]
        SIG_ACK[" ack<br/>应答"]
    end

    CPU_M --> ARB
    CPU_D --> ARB
    DMA --> ARB
    ARB --> MEM_S
    ARB --> UART_S
    ARB --> GPIO_S
    ARB --> TIMER_S
    ARB --> PLIC_S

    style ARB fill:#FF5722,color:#fff
    style CPU_M fill:#2196F3,color:#fff
    style CPU_D fill:#2196F3,color:#fff
    style MEM_S fill:#FF9800,color:#fff
    style UART_S fill:#4CAF50,color:#fff
    style GPIO_S fill:#4CAF50,color:#fff
```

### 5.6 地址空间映射

```mermaid
graph TD
    subgraph 地址空间分配
        R1["0x0000_0000<br/>~0x0000_FFFF<br/>主存 BRAM 64KB"]
        R2["0x0001_0000<br/>~0x0BFF_FFFF<br/>保留"]
        R3["0x0C00_0000<br/>~0x0CFF_FFFF<br/>PLIC 中断控制器"]
        R4["0x1000_0000<br/>~0x1000_000F<br/>UART 串口"]
        R5["0x1001_0000<br/>~0x1001_000F<br/>GPIO"]
        R6["0x1002_0000<br/>~0x1002_000F<br/>Timer 定时器"]
        R7["0x1003_0000<br/>~0x1003_00FF<br/>SPI (可选)"]
    end

    style R1 fill:#FF9800,color:#fff
    style R3 fill:#9C27B0,color:#fff
    style R4 fill:#4CAF50,color:#fff
    style R5 fill:#4CAF50,color:#fff
    style R6 fill:#4CAF50,color:#fff
```

### 5.7 中断处理流程

```mermaid
graph TD
    subgraph 中断源
        TIMER_IRQ["Timer中断<br/>定时器超时"]
        UART_IRQ["UART中断<br/>接收完成"]
        GPIO_IRQ["GPIO中断<br/>按键触发"]
        SW_IRQ["软件中断<br/>CSR触发"]
    end

    subgraph PLIC处理
        PLIC_REQ["PLIC接收中断请求"]
        PLIC_PRI["优先级仲裁"]
        PLIC_ID["生成中断ID"]
        PLIC_CPU["向CPU发送中断"]
    end

    subgraph CPU中断响应
        CPU_SAVE["保存PC和状态<br/>到CSR mepc/mcause"]
        CPU_VEC["跳转到中断向量<br/>地址 mtvec"]
        CPU_HANDLER["执行中断处理程序<br/>读取PLIC Claim寄存器"]
        CPU_SERVICE["服务中断源<br/>读写外设寄存器"]
        CPU_COMPLETE["写PLIC Complete寄存器<br/>完成中断"]
        CPU_RET["mret 返回<br/>恢复PC和状态"]
    end

    TIMER_IRQ & UART_IRQ & GPIO_IRQ & SW_IRQ --> PLIC_REQ
    PLIC_REQ --> PLIC_PRI --> PLIC_ID --> PLIC_CPU
    PLIC_CPU --> CPU_SAVE --> CPU_VEC --> CPU_HANDLER
    CPU_HANDLER --> CPU_SERVICE --> CPU_COMPLETE --> CPU_RET

    style TIMER_IRQ fill:#FF9800,color:#fff
    style PLIC_CPU fill:#9C27B0,color:#fff
    style CPU_SAVE fill:#F44336,color:#fff
    style CPU_RET fill:#4CAF50,color:#fff
```

### 5.8 优缺点分析

| 优点 | 缺点 |
|------|------|
| 完整可运行的计算机系统 | 设计复杂度最高 |
| 满足进阶层次全部要求 | 需要大量调试时间 |
| 可运行真实程序 | Cache一致性需考虑 |
| 性能量化分析有数据支撑 | 总线协议需仔细设计 |
| 可扩展性强 | 资源占用大 |

---

## 6. 系统框图集合

### 6.1 顶层系统架构图

```mermaid
graph TD
    subgraph 软件层
        APP["应用程序<br/>C/汇编程序"]
        BSP["BSP<br/>板级支持包"]
        LIBC["libc<br/>精简C库"]
    end

    subgraph 硬件抽象层
        DRV["驱动程序<br/>UART/GPIO/Timer驱动"]
    end

    subgraph SoC硬件层
        CPU_H["RISC-V CPU<br/>五级流水线"]
        CACHE_H["Cache子系统<br/>I-Cache + D-Cache"]
        BUS_H["总线互联<br/>Bus Matrix"]
        PERI_H["外设控制器<br/>UART/GPIO/Timer/PLIC"]
    end

    subgraph FPGA物理层
        BRAM_H["BRAM<br/>块存储器"]
        PLL_H["PLL/MMCM<br/>时钟管理"]
        IO_H["I/O引脚<br/>FPGA物理接口"]
    end

    APP --> BSP
    BSP --> LIBC
    LIBC --> DRV
    DRV --> PERI_H

    CPU_H --> CACHE_H
    CACHE_H --> BUS_H
    BUS_H --> PERI_H

    PERI_H --> BRAM_H
    CPU_H --> PLL_H
    PERI_H --> IO_H

    style APP fill:#2196F3,color:#fff
    style CPU_H fill:#4CAF50,color:#fff
    style CACHE_H fill:#8BC34A,color:#fff
    style BUS_H fill:#FF5722,color:#fff
    style BRAM_H fill:#FF9800,color:#fff
    style IO_H fill:#607D8B,color:#fff
```

### 6.2 CPU内部模块层级图

```mermaid
graph TD
    subgraph 顶层
        TOP["riscv_top<br/>顶层模块"]
    end

    subgraph CPU核心
        CORE_M["cpu_core<br/>CPU核心模块"]
    end

    subgraph 流水线各级
        IF_M["if_stage<br/>取指模块"]
        ID_M["id_stage<br/>译码模块"]
        EX_M["ex_stage<br/>执行模块"]
        MEM_M["mem_stage<br/>访存模块"]
        WB_M["wb_stage<br/>写回模块"]
    end

    subgraph 流水线寄存器
        IF_ID["if_id_reg<br/>IF/ID寄存器"]
        ID_EX["id_ex_reg<br/>ID/EX寄存器"]
        EX_MEM["ex_mem_reg<br/>EX/MEM寄存器"]
        MEM_WB["mem_wb_reg<br/>MEM/WB寄存器"]
    end

    subgraph 功能模块
        PC_M["pc_reg<br/>PC寄存器"]
        IMEM_M["inst_mem<br/>指令存储器"]
        DMEM_M["data_mem<br/>数据存储器"]
        REG_M["reg_file<br/>寄存器堆"]
        ALU_M["alu<br/>算术逻辑单元"]
        CTRL_M["ctrl_unit<br/>控制单元"]
        IMM_M["imm_gen<br/>立即数生成"]
        HAZ_M["hazard_unit<br/>冒险检测"]
        FWD_M["forward_unit<br/>前递单元"]
        BR_M["branch_unit<br/>分支单元"]
    end

    TOP --> CORE_M
    CORE_M --> IF_M & ID_M & EX_M & MEM_M & WB_M
    IF_M --> IF_ID --> ID_M
    ID_M --> ID_EX --> EX_M
    EX_M --> EX_MEM --> MEM_M
    MEM_M --> MEM_WB --> WB_M

    IF_M --> PC_M & IMEM_M
    ID_M --> REG_M & CTRL_M & IMM_M & HAZ_M
    EX_M --> ALU_M & FWD_M & BR_M
    MEM_M --> DMEM_M
    WB_M --> REG_M

    style TOP fill:#2196F3,color:#fff
    style CORE_M fill:#4CAF50,color:#fff
    style HAZ_M fill:#FF5722,color:#fff
    style FWD_M fill:#FF5722,color:#fff
    style ALU_M fill:#9C27B0,color:#fff
    style CTRL_M fill:#FF9800,color:#fff
```

### 6.3 数据通路完整流程图

```mermaid
graph LR
    subgraph 取指IF
        PC["PC<br/>程序计数器"]
        IMEM["指令存储器<br/>取指令"]
        NPC["下一条PC<br/>计算"]
    end

    subgraph 译码ID
        DEC["指令译码<br/>生成控制信号"]
        RF_R["寄存器读<br/>rs1, rs2"]
        IMM["立即数<br/>解码"]
    end

    subgraph 执行EX
        ALU["ALU<br/>运算"]
        BR["分支判定<br/>条件判断"]
        JAL["跳转地址<br/>计算"]
    end

    subgraph 访存MEM
        DMEM["数据存储器<br/>读/写"]
        LD["Load<br/>数据读取"]
        ST["Store<br/>数据写入"]
    end

    subgraph 写回WB
        MUX_WB{"数据选择"}
        RF_W["寄存器写<br/>rd"]
    end

    PC --> IMEM --> DEC
    DEC --> RF_R
    DEC --> IMM
    RF_R --> ALU
    IMM --> ALU
    ALU --> DMEM
    DMEM --> MUX_WB
    ALU --> MUX_WB
    MUX_WB --> RF_W

    ALU --> BR
    BR --> NPC
    JAL --> NPC
    PC --> NPC
    NPC --> PC

    style PC fill:#2196F3,color:#fff
    style ALU fill:#4CAF50,color:#fff
    style IMEM fill:#FF9800,color:#fff
    style DMEM fill:#FF9800,color:#fff
    style RF_R fill:#9C27B0,color:#fff
    style RF_W fill:#9C27B0,color:#fff
```

### 6.4 开发流程与验证路线图

```mermaid
graph TD
    subgraph 第一阶段_基础层次
        P1["模块级开发"]
        P1a["ALU设计与验证"]
        P1b["寄存器堆设计与验证"]
        P1c["指令存储器/数据存储器"]
        P1d["控制器设计"]

        P2["单周期CPU集成"]
        P2a["数据通路连接"]
        P2b["控制器集成"]
        P2c["功能仿真验证"]

        P3["基础层次硬件验证"]
        P3a["综合 Synthesis"]
        P3b["布局布线 P&R"]
        P3c["比特流生成 Bitstream"]
        P3d["FPGA上板测试"]
    end

    subgraph 第二阶段_进阶层次
        P4["流水线改造"]
        P4a["添加流水线寄存器"]
        P4b["冒险检测单元"]
        P4c["前递单元"]
        P4d["流水线仿真验证"]

        P5["存储系统集成"]
        P5a["I-Cache设计"]
        P5b["D-Cache设计"]
        P5c["总线设计"]
        P5d["存储系统验证"]

        P6["外设集成"]
        P6a["UART控制器"]
        P6b["GPIO控制器"]
        P6c["Timer控制器"]
        P6d["中断控制器PLIC"]

        P7["系统级联调"]
        P7a["SoC集成"]
        P7b["启动程序测试"]
        P7c["性能量化评估"]
        P7d["进阶层次验收"]
    end

    P1a & P1b & P1c & P1d --> P2a
    P2a --> P2b --> P2c
    P2c --> P3a --> P3b --> P3c --> P3d

    P3d --> P4a
    P4a --> P4b --> P4c --> P4d
    P4d --> P5a
    P5a --> P5b --> P5c --> P5d
    P5d --> P6a
    P6a --> P6b --> P6c --> P6d
    P6d --> P7a --> P7b --> P7c --> P7d

    style P1 fill:#4CAF50,color:#fff
    style P2 fill:#4CAF50,color:#fff
    style P3 fill:#4CAF50,color:#fff
    style P4 fill:#FF9800,color:#fff
    style P5 fill:#FF9800,color:#fff
    style P6 fill:#FF9800,color:#fff
    style P7 fill:#FF9800,color:#fff
```

### 6.5 测试验证策略图

```mermaid
graph TD
    subgraph 仿真验证
        S1["单元测试<br/>Unit Test"]
        S2["集成测试<br/>Integration Test"]
        S3["系统测试<br/>System Test"]
        S4["回归测试<br/>Regression Test"]

        S1 --> S2 --> S3 --> S4
    end

    subgraph 测试方法
        T1["定向测试<br/>针对每条指令"]
        T2["随机测试<br/>随机指令序列"]
        T3["自检测试<br/>程序自检结果"]
        T4["对比测试<br/>与参考模型对比"]
        T5["覆盖率测试<br/>功能覆盖率分析"]
    end

    subgraph 参考模型
        R1["Spike ISA模拟器<br/>官方参考模型"]
        R2["RISC-V Tests<br/>官方测试集"]
        R3["自写Reference Model<br/>Python/C模型"]
    end

    subgraph 硬件验证
        H1["综合后仿真<br/>Post-Synthesis Sim"]
        H2["布局布线后仿真<br/>Post-P&R Sim"]
        H3["时序分析<br/>Timing Analysis"]
        H4["资源利用报告<br/>Utilization Report"]
        H5["上板验证<br/>FPGA On-board Test"]
    end

    S1 --> T1 & T2
    S2 --> T3 & T4
    S3 --> T5
    T4 --> R1 & R2 & R3

    S3 --> H1 --> H2 --> H3
    H3 --> H4 --> H5

    style S1 fill:#4CAF50,color:#fff
    style S3 fill:#4CAF50,color:#fff
    style T4 fill:#2196F3,color:#fff
    style R1 fill:#9C27B0,color:#fff
    style H5 fill:#FF9800,color:#fff
```

---

## 7. 方案选择建议

### 7.1 推荐方案路线

```mermaid
graph LR
    subgraph 推荐路线
        R1["步骤1<br/>单周期CPU<br/>(方案一)"]
        R2["步骤2<br/>五级流水线<br/>(方案三)"]
        R3["步骤3<br/>完整SoC<br/>(方案四)"]
    end

    R1 -->|验证ISA正确性| R2
    R2 -->|集成存储与外设| R3

    style R1 fill:#4CAF50,color:#fff
    style R2 fill:#FF9800,color:#fff
    style R3 fill:#FF9800,color:#fff
```

### 7.2 推荐理由

| 决策点 | 推荐选择 | 理由 |
|--------|---------|------|
| 基础层次架构 | 方案一（单周期） | 先验证ISA正确性，降低风险 |
| 是否做多周期 | 跳过（方案二） | 单周期已验证ISA，直接进入流水线 |
| 进阶层次架构 | 方案三（五级流水线） | 课程要求引入流水线机制 |
| 完整系统 | 方案四（SoC） | 满足"完整可运行系统"要求 |
| Cache策略 | 直接映射 + 写回 | FPGA BRAM资源适中，实现简洁 |
| 总线协议 | Wishbone B4 | 协议简单，文档清晰，适合教学 |
| 分支策略 | ID阶段判定 + 静态预测 | 平衡复杂度与性能 |

### 7.3 关键技术决策

```mermaid
graph TD
    subgraph ISA决策
        ISA1["指令集: RV32I<br/>37条基础指令"]
        ISA2["可选扩展: M扩展<br/>mul/div/rem"]
        ISA3["暂不支持: F/D/A/C<br/>浮点/原子/压缩"]
    end

    subgraph 微架构决策
        MA1["流水线深度: 5级<br/>IF-ID-EX-MEM-WB"]
        MA2["分支处理: ID阶段判定<br/>静态预测(BTFN)"]
        MA3["前递: EX→EX, MEM→EX<br/>两条前递路径"]
        MA4["Load-Use: 1周期停顿<br/>检测+bubble插入"]
    end

    subgraph 存储决策
        MEM1["I-Cache: 4KB<br/>直接映射, 16B行"]
        MEM2["D-Cache: 4KB<br/>直接映射, 写回"]
        MEM3["主存: BRAM 64KB<br/>FPGA片上存储"]
        MEM4["总线: Wishbone B4<br/>Classic模式"]
    end

    subgraph 外设决策
        PER1["UART: 115200 baud<br/>连接板载USB-UART"]
        PER2["GPIO: 16位<br/>LED/开关/按键"]
        PER3["Timer: 32位<br/>可产生周期中断"]
        PER4["PLIC: 简化版<br/>支持4个中断源"]
    end

    style ISA1 fill:#2196F3,color:#fff
    style MA1 fill:#4CAF50,color:#fff
    style MEM1 fill:#FF9800,color:#fff
    style PER1 fill:#9C27B0,color:#fff
```

### 7.4 团队分工建议

```mermaid
graph TD
    subgraph 4人团队分工
        M1["成员A<br/>CPU核心<br/>(ALU+RegFile+Ctrl)"]
        M2["成员B<br/>流水线与冒险<br/>(Pipeline+Fwd+Hazard)"]
        M3["成员C<br/>存储系统<br/>(Cache+Bus+Memory)"]
        M4["成员D<br/>外设与SoC<br/>(UART+GPIO+PLIC+集成)"]
    end

    subgraph 阶段一_基础层次
        PH1_A["A: ALU+RegFile"]
        PH1_B["B: 单周期数据通路"]
        PH1_C["C: 存储器+仿真Testbench"]
        PH1_D["D: 控制器+集成测试"]
    end

    subgraph 阶段二_进阶层次
        PH2_A["A: 流水线IF/ID级"]
        PH2_B["B: 流水线EX级+前递"]
        PH2_C["C: Cache设计+总线"]
        PH2_D["D: 外设+SoC集成+测试"]
    end

    M1 --> PH1_A & PH2_A
    M2 --> PH1_B & PH2_B
    M3 --> PH1_C & PH2_C
    M4 --> PH1_D & PH2_D

    style M1 fill:#2196F3,color:#fff
    style M2 fill:#4CAF50,color:#fff
    style M3 fill:#FF9800,color:#fff
    style M4 fill:#9C27B0,color:#fff
```

---

## 附录：Mermaid 图表索引

| 图表编号 | 图表名称 | 所在章节 |
|---------|---------|---------|
| 1 | 架构演进路线 | 1.2 |
| 2 | 整体设计层次结构 | 1.3 |
| 3 | 单周期数据通路框图 | 2.2 |
| 4 | 单周期控制信号生成 | 2.3 |
| 5 | ALU功能与指令类型 | 2.4 |
| 6 | 多周期控制器状态机 | 3.2 |
| 7 | 多周期数据通路框图 | 3.3 |
| 8 | 指令周期数分配 | 3.4 |
| 9 | 五级流水线架构总图 | 4.2 |
| 10 | 数据冒险与前递路径 | 4.3 |
| 11 | 前递逻辑详解 | 4.4 |
| 12 | 流水线时序图 | 4.5 |
| 13 | 分支处理策略 | 4.6 |
| 14 | SoC系统顶层框图 | 5.2 |
| 15 | 存储层次结构 | 5.3 |
| 16 | Cache结构设计 | 5.4 |
| 17 | 总线协议与互联 | 5.5 |
| 18 | 地址空间映射 | 5.6 |
| 19 | 中断处理流程 | 5.7 |
| 20 | 顶层系统架构图 | 6.1 |
| 21 | CPU内部模块层级图 | 6.2 |
| 22 | 数据通路完整流程图 | 6.3 |
| 23 | 开发流程与验证路线图 | 6.4 |
| 24 | 测试验证策略图 | 6.5 |
| 25 | 推荐方案路线 | 7.1 |
| 26 | 关键技术决策 | 7.3 |
| 27 | 团队分工建议 | 7.4 |

---

> 本文档包含4种架构方案和27个Mermaid系统框图，覆盖基础层次与进阶层次的全部设计需求。
> 所有Mermaid代码均可在支持Mermaid渲染的Markdown编辑器（如VS Code + Mermaid插件、Typora）中直接预览。
