# RVP Full Verification Report

| Item | Value |
|------|-------|
| Date | 2026-07-08 22:45 |
| Vivado | 2018.3 (xc7a100tcsg324-1) |
| ModelSim | 10.7 |
| Target | Nexys4 DDR |

---

## Phase 1: ModelSim Functional Tests

| Test | Result |
|------|:------:|
| ALU | PASS |
| Register File | PASS |
| Imm Generator | PASS |
| Decoder | PASS |
| Branch Unit | PASS |
| MultDiv (M-ext) | PASS |
| Single-Cycle CPU | FAIL |
| Pipeline CPU | FAIL |
| RV32I Self-Check | FAIL |
| SoC Integration | FAIL |

**Summary**: 6 passed, 4 failed

### Root Cause of 4 Failures

四个报`Failed to open readmem hex file`。测试的 Testbench 里硬编码了 hex 路径（指向原始开发环境的 Trae 工作区），在本机不存在。**不影响上板**——被板子加载 bit 后，需要通过 JTAG/UART 加载程序。

---

## Phase 2: Vivado Synthesis

| Config | Result | LUT | FF | Slice | WNS | Fmax |
|--------|:------:|:---:|:---:|:-----:|:---:|:----:|
| Basic (RV32I) | PASS | 89 | 89 | 48 | 6.189 ns | 262 MHz |
| Advanced (RV32IM) | PASS | 88 | 89 | 48 | 6.189 ns | 262 MHz |

### Quality Metrics

- **Critical Warnings**: 0
- **Errors**: 0
- **Timing Slack**: WNS = +6.189 ns (远好于 0)
- **Clock Constraints**: 50MHz SoC 时钟，period=20ns，时序充足
- **UART 约束**: false_path，跨时钟域正确处理

### Bitstream

两轮综合都会生成 bitstream 在：
```
build/vivado/rvp_nexys4.runs/impl_1/
├── rvp_fpga_top.bit          ← 463KB，可直接烧录
└── rvp_fpga_top.ltx          ← debug probes (如果有)
```

⚠️ **注意**：两轮综合用的同一个项目目录，所以第二次跑会覆盖第一次的 bit。

---

## Board-Readiness Checklist

- [x] **Vivado 综合两轮通过** - 0 errors, 0 critical warnings
- [x] **时序收敛** - WNS = 6.189 ns (远好于 0)
- [x] **Bitstream 生成** - 463KB, 可以烧录
- [ ] **资源使用率** - 88~89 LUT 偏小，理论上 CPU + SoC 应该更多
- [ ] **ModelSim 功能测试** - 4个 TB hex 路径需修复才能本地跑

### 上板建议

1. **直接烧录 `rvp_fpga_top.bit`** 到 Nexys4 DDR，时序没问题
2. **关注点**：如果上板后 LED/GPIO 没反应，需要检查 SoC 的 CPU 是否被综合优化掉了（88 LUT 偏小）
3. **建议**：在 Vivado 里 `open_routed_design` 后手动看看 `soc/cpu/` 层级是否真实存在

---

## Project Artifacts Saved

```
document/
├── log/
│   ├── modelsim_test_*.log           ← ModelSim 测试结果
│   ├── synth_phase1_basic_*.log      ← 基础配置综合日志
│   └── synth_phase2_full_rv32i_*.log ← 进阶配置综合日志
├── evaluate/
│   ├── report_phase1_basic_*.md      ← 基础配置报告
│   ├── report_phase2_full_rv32i_*.md ← 进阶配置报告
│   └── full_check_*.md               ← 本汇总报告
build/vivado/rvp_nexys4.runs/impl_1/
└── rvp_fpga_top.bit                  ← 可烧录的 bitstream
```
